module.exports = {
  apps : [{
    name: "{{ item.name }}",
    script: "app.js",
    cwd: "{{ item.webroot }}",
    autorestart: true,
    watch: false,
    env: {
      NODE_ENV: "development",
    },
    env_production: {
      NODE_ENV: "production",
    }
  }]
}
