- name: restart socket
  systemd:
    name: "{{process_name}}.socket"
    state: stopped
    daemon_reload: yes
  when: use_socket

- name: restart service
  systemd:
    name: "{{process_name}}.service"
    state: restarted
    daemon_reload: yes
