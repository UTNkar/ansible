---
admin_groups: 'sudo'

# Users
users:
  # Administrators
  - name: "jdekker"
    groups: "{{ admin_groups }}"
    home: "/home/jdekker"
    shell: "/bin/bash"
    ssh_pubkey: "{{ lookup('file', 'files/pubkeys/jdekker.pub') }}"
    hosts:
      - "turing.utn.se"
      - "babbage.utn.se"
      - "moore.utn.se"
  # Committees
  - name: "balen"
    groups: ""
    home: "/var/www/balen.utn.se"
    shell: "/bin/bash"
    ssh_pubkey: "{{ lookup('file', 'files/pubkeys/balen.pub') }}"
    hosts:
      - "babbage.utn.se"
  - name: "forsranningen"
    groups: ""
    home: "/var/www/forsranningen.utn.se"
    shell: "/bin/bash"
    ssh_pubkey: "{{ lookup('file', 'files/pubkeys/forsranningen.pub') }}"
    hosts:
      - "babbage.utn.se"
  - name: "polhacks"
    groups: ""
    home: "/var/www/polhacks.utn.se"
    shell: "/bin/bash"
    ssh_pubkey: "{{ lookup('file', 'files/pubkeys/polhacks.pub') }}"
    hosts:
      - "babbage.utn.se"
  - name: "utnarm"
    groups: ""
    home: "/var/www/utnarm.utn.se"
    shell: "/bin/bash"
    ssh_pubkey: "{{ lookup('file', 'files/pubkeys/utnarm.pub') }}"
    hosts:
      - "babbage.utn.se"
  # Sections
  - name: "grus"
    groups: ""
    home: "/var/www/grus.utn.se"
    shell: "/bin/bash"
    ssh_pubkey: "{{ lookup('file', 'files/pubkeys/grus.pub') }}"
    hosts:
      - "babbage.utn.se"
  - name: "h"
    groups: ""
    home: "/var/www/h.utn.se"
    shell: "/bin/bash"
    ssh_pubkey: "{{ lookup('file', 'files/pubkeys/h.pub') }}"
    hosts:
      - "babbage.utn.se"
  - name: "k"
    groups: ""
    home: "/var/www/k.utn.se"
    shell: "/bin/bash"
    ssh_pubkey: "{{ lookup('file', 'files/pubkeys/k.pub') }}"
    hosts:
      - "babbage.utn.se"
  - name: "mfk"
    groups: ""
    home: "/var/www/mfk.utn.se"
    shell: "/bin/bash"
    ssh_pubkey: "{{ lookup('file', 'files/pubkeys/mfk.pub') }}"
    hosts:
      - "turing.utn.se"
  - name: "nvm"
    groups: ""
    home: "/var/www/nvm.utn.se"
    shell: "/bin/bash"
    ssh_pubkey: "{{ lookup('file', 'files/pubkeys/nvm.pub') }}"
    hosts:
      - "turing.utn.se"
  - name: "sts"
    groups: ""
    home: "/var/www/sts.utn.se"
    shell: "/bin/bash"
    ssh_pubkey: "{{ lookup('file', 'files/pubkeys/sts.pub') }}"
    hosts:
      - "babbage.utn.se"
  - name: "w"
    groups: ""
    home: "/var/www/w.utn.se"
    shell: "/bin/bash"
    ssh_pubkey: "{{ lookup('file', 'files/pubkeys/w.pub') }}"
    hosts:
      - "babbage.utn.se"


old_users: []
